# Secrets Template
# Copy this to secrets.yaml (gitignored) and fill in your values

# Memory API (default: localhost)
memory_api:
  url: "http://127.0.0.1:8030"

# Core LLM Credentials
llm:
  # Primary LLM endpoint (for codehammer, interrogator)
  primary:
    url: "http://127.0.0.1:8000/v1"
    model: "nvidia/MiniMax-M2.5-NVFP4"
    api_key: "sk-dummy"
  
  # Alternate LLM endpoint (for chaos, testing)
  alternate:
    url: "http://127.0.0.1:8000/v1"
    model: "nvidia/MiniMax-M2.5-NVFP4"
    api_key: "sk-dummy"